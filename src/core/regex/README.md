# Regex Module

This module handles regex pattern parsing, compilation, and integration with the
automaton system. It translates regex syntax into executable automaton structures.

## Components:
- Pattern parsing and validation
- Regex flag management
- Regex-to-automaton compilation
- Standard regex API compatibility
