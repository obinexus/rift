# Engine Module

This module provides the runtime execution environment for pattern matching using
the automaton structures generated from regex patterns.

## Components:
- Pattern matching execution
- Input stream processing
- Capture group tracking
- Performance optimization
